# Product

## Register

product

## Users

个人创作者、小型影视团队和短剧制作者，在可信的本地或私有部署环境中使用画布，将小说、剧本、角色、风格、参考素材和生成配置组织为可执行的图像、视频、音频工作流。

## Product Purpose

影策把故事从文字推进到可见、可听的镜头。用户需要在同一张自由画布中快速整理节点、连接上下游输入、生成和重试媒体，并在分镜与导演控制之间保持素材和意图的连续性。成功标准是减少重复配置和节点操作，同时让每条连接、每次生成和失败恢复都可见、可控。

## Brand Personality

专业、直接、可控。界面应帮助创作者集中于故事和镜头决策，不用营销式装饰、隐藏状态或复杂术语增加负担。

## Anti-references

不要做成营销落地页、消费级图片瀑布流、堆叠卡片式 SaaS 仪表盘或只靠隐含手势操作的节点编辑器。不要用持续装饰动画、低对比度文字或不可恢复的批量操作掩盖工作状态。

## Design Principles

- 让创作者在画布上直接完成主要工作，常用动作距离选区和节点很近。
- 用可见的输入、输出、预览和结果状态表达工作流，不依赖用户猜测。
- 批量动作必须可预览、可取消、可部分成功，并说明被跳过的原因。
- 保留影视语义：分镜行、角色、风格、参考图和生成配置在连接时不丢失。
- 默认降低认知负担，高级控制仍可展开，不用隐藏关键能力。

## Accessibility & Inclusion

遵循键盘可达和清晰焦点样式；批量动作提供 Escape 取消和失焦取消；不要只用颜色表达目标状态；尊重 `prefers-reduced-motion`；中文界面中的状态、错误和恢复动作必须可读且明确。
