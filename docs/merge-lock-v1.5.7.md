# 锁版记录：A线同步 → origin/main HEAD（批次七开场，2026-09-22，用户裁定后修订）

本文件只记录**实测事实**，不含裁决；裁取决议见 `docs/merge-ruling-cards/`（签字制）。

## 已确认

| 项 | 值 |
| --- | --- |
| 我方 HEAD | `bb1db672`（F-06 合入 + Agent 落位三笔修复：媒体新建锚定 / 复用重定位 / 无引用按视野） |
| 合并目标（**用户裁定：选 2**） | `origin/main` HEAD = **`6f452405`**（fetch 后现值，不再追后续新提交） |
| 参考 tag | v1.5.7 = `0e3fb79b`（HEAD 之前 25 提交） |
| merge-base(HEAD, v1.5.7) | `8d60a516` |
| 上游待合提交数 | **129**（`8d60a516..origin/main`，实测与控制线 129 一致）；其中 `v1.5.7..origin/main` 25 提交 |
| 我方独有提交数 | **566**（`v1.5.7..HEAD`） |
| `v1.5.6` tag | `473d535e`；`v1.5.6..v1.5.7` = **34** 提交 |
| 侦察基线提交 `d87c3944` | `d87c3944..v1.5.7` = **13** 提交（该提交是 v1.5.6 的合并点，故与 tag 口径差 21） |

## 口径已澄清（用户 2026-09-22 裁定：控制线 129/49/三提交位置均为 HEAD 口径）

**裁定**：锁 `origin/main` HEAD = `6f452405`，129 提交区间，`b8eefdad`（模型选择彩色标签）/`c5b81f69`（Live2D）/`6f452405`（画布批量生成）三个重点提交**纳入本次增量侦察**；v1.5.7 tag 仅作参考点。

原控制线口径与实际 tag 口径的差异（供后续复核口径来源）：

- `8d60a516..v1.5.7` = **104**（非 129）；`d87c3944..v1.5.7` = **13**（非 49）。
- **`b8eefdad`（模型选择彩色标签）、`c5b81f69`（Live2D）、`6f452405`（画布批量生成）都不在 v1.5.7 内**，仅存在于 `origin/main`（tag 之后另有 **25** 提交）。

即：若严格按「目标 = v1.5.7、不追 HEAD」，这三个"重点预判"对象**不在本次合并范围**（属下次同步）；若要预判它们，合并目标需改为 `origin/main` HEAD，与控制线「不追 HEAD」冲突。二者需择一。

## 待办（批次七剩余，需新会话执行）

1. 开场自校准读取：`docs/upstream-sync-recon-v1.5.1-v1.5.6.md`、`docs/merge-ruling-cards/` 九张、journal 顶部。
2. 增量侦察：`d87c3944..v1.5.7`（或按裁定改为 `..origin/main`）三方模拟 + 冲突热区对照 → 新增/修订裁决卡。
3. 我方 delta 审计：侦察基线..`bb1db672`（canvas-node.tsx 已叠 F-06、`cloud_agent_media.go` 前进 75 行、`outpaintImageNode` 提交链进入 canvas_state 域）。
4. 先遣队重算：80 提交时代的 19 提交先遣队作废，按新地图重算零碰撞边界。
