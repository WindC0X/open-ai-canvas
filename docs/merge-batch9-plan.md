# 批次九执行计划：A线 v1.5.1→origin/main 合并（2026-09-22 起）

> 依据：已签字的 `docs/merge-signature-sheet.md`（E 区）+ 全量冲突图 `docs/merge-conflict-map-129.md`（70 冲突提交 / 627 hunk）。
> 纪律：**merge 式（非 rebase）**、逐波收尾落盘、逐冲突按卡处置、每个合并提交信息留证；禁止 `reset --hard`／`checkout --`；逐文件 `git add`。

## 一、环境（已就绪）

| 项 | 值 |
| --- | --- |
| 合并 worktree | `/mnt/f/CODE/Project/oac-wt-merge-v157`（分支 `merge-v1.5.7`） |
| 起点 | `7ca75afc`（= main，含批次七/八产物） |
| 合并目标 | `origin/main` HEAD **`6f452405`**（锁版，不追后续） |
| merge-base | `8d60a516` |
| 备份 | 分支 `backup/main-pre-v157-merge`（+ 既有 `backup/main-pre-merge-v150` 等） |
| rerere | 已开（enabled + autoUpdate） |
| node_modules | 软链自主 checkout（web/docs），供 tsc/build 验证 |

## 二、关键实测事实（决定手法）

1. **无干净前缀**：`8d60a516..origin/main` 的第一个提交 `772c5519` 即冲突（globals.css dark .ant-message 强制色，10 hunk）。
   → "先遣队先行"在顺序合并中的正确含义 = **轮到零碰撞提交时零处置**（书签用），不是可以先跳着合。
2. **冲突分布**：129 提交中 70 个有冲突；重灾 `6abaa7b4`(36, PoloX) / `9148ceab`(33, 工作区整合) / `dc1ad680`(31) / `35ebed30`(30, 删除 patch) / `6f452405`(26) / `b8eefdad`(25) / `17ffdda0`(25) / `33908ed1`(21) / `a3bf0bd8`(21) / `19c9a207`(18) / `adf3a5be`(17) / `237f2e2a`(17) / `6d355380`(17)。
3. **两大文件战场**：`web/src/pages/canvas/project.tsx`（我方 71 次热改，多提交各压 11–13 hunk）与 `web/src/components/model-picker.tsx`（我方 813 行重写，上游 37 hunk）。

## 三、波次划分（每波 = 一段上游区间 + 收尾落盘）

| 波 | 区间（至该上游提交为止） | 覆盖冲突提交 | 处置卡 |
| --- | --- | --- | --- |
| **W1** | → `9eb2fcd4`（v1.5.1 release） | `772c5519`(10)、`35ebed30`(30)、`a3b6761b`(12)、`025b5e84`(14)、`9eb2fcd4`(16)、其余 11 个 1–5 hunk | **卡 05**（删除 patch 语义）、**卡 06**（同步/水位门，含前置重核）、**卡 07**（undone 刷新）、**卡 09**（globals.css/banner 对账） |
| **W2** | → `9b9a7f4a`（PoloX 融合） | `6abaa7b4`(36)、`8e3692ea`(13)、`9b9a7f4a`(5) | 按树态一次解决（批次〇已定：勿逐提交重放）；新写工具 `image_layer_split` 入撤销/step_hash 白名单（卡 02 关联） |
| **W3** | → `237f2e2a`（模型目录收窄） | `531019b3`(15)、`2e509228`(15)、`0a06783f`(16)、`237f2e2a`(17)、`46a32a95`(5) | **卡 04**（模型选择校验/参数错误）、**卡 08**（frontend 目录来源，含运维前置）、**卡 09**（outline 策略采纳） |
| **W4** | → `9148ceab`（工作区整合） | `17ffdda0`(25)、`33908ed1`(21)、`cfcc53a9`(13)、`5eab8126`(10)、`9148ceab`(33)、`16bd4b92`(2) | **卡 09**（样式拆分随迁）、**卡 04**、W3 flora-tokens 加载序相关（同步后核） |
| **W5** | → `adf3a5be`/`0ea9f7d9`（Agent 执行链） | `adf3a5be`(17)、`0ea9f7d9`(12)、`506f463d`(4) | **卡 01**（hash 三口径）、**卡 02**（step_hash 融合）、**卡 03**（runtime 409 嫁接）、**卡 04** |
| **W6** | → `a3bf0bd8`（多模态表格 PR#549） | `d87c3944`(16)、`6d355380`(17)、`a3bf0bd8`(21)、`5843d489`(15)、`7985dbec`(12)、`27bcca62`(11)、`e921deac`(3)、`cf4d2ab1`(2) | **卡 10**（先读 `canvas-consistency-repair.mdx` 定口径）、**卡 12**（92 hunk 预先语义分析：纯新增直收 vs 压我方改动） |
| **W7** | → `6f452405`（终波） | `b8eefdad`(25)、`19c9a207`(18)、`dc1ad680`(31)、`c5b81f69`(7)、`6f452405`(26)、`85de9907`(11)、`8b8ea291`(7)、`13707c06`(4)、`fd3fc44d`(2)、另小项 | **卡 11**（标签域）、**卡 10**、**卡 04 附条件**（我方 prepare 清零 + 上游回给模型/有限重试，限额按上游默认，提交信息留证）、**卡 06 前置重核落盘** |

## 四、每波收尾门（固定四件）

1. `cd web && bunx tsc --noEmit`（0 error）→ `bun run build`（通过）。
2. `cd backend && go build ./...`（通过）；涉及 backend 行为的波补 `go test ./internal/app/...`。
3. 卡要求的**证据落盘**：附条件卡的条件兑现证据（卡 04 重试上限留证、卡 06 重核结论、卡 08 运维前置确认、卡 10 口径结论、卡 12 分类结论）。
4. 合并提交信息格式：`merge(upstream): <区间> - <波内要点>；处置卡：01/05/09…；门：tsc/build/go 结果`。

## 五、冲突处置通则

- 逐 hunk 读**双方意图**再取边；卡已定方向的按卡执行；卡未覆盖的**停手报告**，不自行调和。
- 我方 fork-only 能力（HUD 让位、批链收敛、撤销复活守卫、flora 样式直改值）为**必保**项；上游结构优先、我方语义重放（卡 05/06/07 已示此模式）。
- 每波结束若有未决 hunk，**不提交半成品**：回退该波（`git merge --abort` 或停在干净点）并报告。
